[project]
name = "github-mcp-test"
version = "0.1.0"
description = "Use a repository-scoped GitHub App token with GitHub's remote MCP server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyjwt[crypto]>=2.13.0",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]
